grafana:
  proxyProvider: default
  ports:
    443/https:
      targets:
        - http://127.0.0.1:3000
  dashboard:
    label: Grafana
    icon: si/grafana

victoriametrics:
  proxyProvider: default
  ports:
    443/https:
      targets:
        - http://127.0.0.1:8428
  dashboard:
    label: VictoriaMetrics
    icon: si/victoriametrics

victorialogs:
  proxyProvider: default
  ports:
    443/https:
      targets:
        - http://127.0.0.1:9428
  dashboard:
    label: VictoriaLogs
    icon: si/victorialogs

searxng:
  proxyProvider: default
  ports:
    443/https:
      targets:
        - http://127.0.0.1:8888
  dashboard:
    label: SearXNG
    icon: si/magnifyingglass

9router:
  proxyProvider: default
  ports:
    443/https:
      targets:
        - http://127.0.0.1:20128
  dashboard:
    label: 9Router

hermes:
  proxyProvider: default
  ports:
    443/https:
      targets:
        - http://127.0.0.1:9119
  dashboard:
    label: Hermes Dashboard
    icon: si/robot

immich:
  proxyProvider: default
    hostname: gallery
  ports:
    443/https:
      targets:
        - http://127.0.0.1:2283
  dashboard:
    label: Immich
